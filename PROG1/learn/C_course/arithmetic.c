#include <stdio.h>

int main() {

    // arithmetic operators = + - * / % ++ --

    int x = 10;
    int y = 3;
    int z = 0;

    //z = x + y;
    //z = x - y;
    //z = x * y;
    //z = x / y; // CAUTION: the variable that is dividing the other and the one that will store the result, MUST BE FLOAT!!
    //z = x % y; // get the reminder
    //z++;
    //z--;

    //augmented assingment operators
    // x+=3;
    // x-=3;
    // x*=2;
    // x/=2;

    printf("%d\n", z);

}
